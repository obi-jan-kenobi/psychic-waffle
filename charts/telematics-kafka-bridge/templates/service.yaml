apiVersion: v1
kind: Service
metadata:
  name: {{ include "telematics-kafka-bridge.fullname" . }}
  labels:
    {{- include "telematics-kafka-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telematics-kafka-bridge.selectorLabels" . | nindent 4 }}
