apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-worker.fullname" . }}
  labels:
    {{- include "sample-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-worker.selectorLabels" . | nindent 4 }}
